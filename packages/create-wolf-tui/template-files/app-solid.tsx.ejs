import { createSignal, createMemo } from 'solid-js'
import { Box, Text, useInput, useApp } from '@wolf-tui/solid'

export default function App() {
	const [count, setCount] = createSignal(0)
	const { exit } = useApp()

	useInput((input, key) => {
		if (key.upArrow) setCount((c) => c + 1)
		if (key.downArrow) setCount((c) => c - 1)
		if (input === 'q') exit()
	})
<% if (vars.cssFlavor === 'tailwind') { %>
	const borderColor = createMemo(() =>
		count() > 0 ? '<%= vars.tw.borderPositive %>' : count() < 0 ? '<%= vars.tw.borderNegative %>' : '<%= vars.tw.borderNeutral %>'
	)
	const textColor = createMemo(() =>
		count() > 0 ? '<%= vars.tw.colorPositive %>' : count() < 0 ? '<%= vars.tw.colorNegative %>' : '<%= vars.tw.colorNeutral %>'
	)

	return (
		<Box className="flex-col <%= vars.tw.outerBorder %> <%= vars.tw.borderBrand %> px-2 py-1 gap-1 w-[40]">
			<Box className="justify-center">
				<Text className="font-bold <%= vars.tw.colorBrand %>">wolf-tui</Text>
			</Box>

			<Box className={`justify-center <%= vars.tw.innerBorder %> px-2 ${borderColor()}`}>
				<Text className={`font-bold ${textColor()}`}>{count()}</Text>
			</Box>

			<Box className="justify-around">
				<Text className="<%= vars.tw.colorPositive %>">↑ up</Text>
				<Text className="<%= vars.tw.colorNegative %>">↓ down</Text>
				<Text className="<%= vars.tw.colorMuted %>">q quit</Text>
			</Box>
		</Box>
	)
<% } else if (vars.cssImport) { %>
	const displayClass = createMemo(() =>
		count() > 0
			? 'display display-positive'
			: count() < 0
				? 'display display-negative'
				: 'display display-zero'
	)

	return (
		<Box className="counter">
			<Box className="title">
				<Text className="label">wolf-tui</Text>
			</Box>

			<Box className={displayClass()}>
				<Text>{count()}</Text>
			</Box>

			<Box className="controls">
				<Text className="key-up">↑ up</Text>
				<Text className="key-down">↓ down</Text>
				<Text className="key-quit">q quit</Text>
			</Box>
		</Box>
	)
<% } else { %>
	const color = createMemo(() =>
		count() > 0 ? 'green' : count() < 0 ? 'red' : 'white'
	)

	return (
		<Box
			style={{
				'flex-direction': 'column',
				'border-style': 'round',
				'border-color': 'cyan',
				'padding-left': 2,
				'padding-right': 2,
				'padding-top': 1,
				'padding-bottom': 1,
				gap: 1,
				width: 40,
			}}
		>
			<Box style={{ 'justify-content': 'center' }}>
				<Text style={{ 'font-weight': 'bold', color: 'cyan' }}>wolf-tui</Text>
			</Box>

			<Box
				style={{
					'justify-content': 'center',
					'border-style': 'single',
					'border-color': color(),
					'padding-left': 2,
					'padding-right': 2,
				}}
			>
				<Text style={{ 'font-weight': 'bold', color: color() }}>{count()}</Text>
			</Box>

			<Box style={{ 'justify-content': 'space-around' }}>
				<Text style={{ color: 'green' }}>↑ up</Text>
				<Text style={{ color: 'red' }}>↓ down</Text>
				<Text style={{ color: 'gray' }}>q quit</Text>
			</Box>
		</Box>
	)
<% } %>
}
